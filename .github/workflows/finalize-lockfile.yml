name: Finalize frontend lockfile

on:
  workflow_dispatch:
  push:
    branches:
      - chore/frontend-lockfile

permissions:
  contents: write

concurrency:
  group: finalize-frontend-lockfile
  cancel-in-progress: true

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
      - name: Generate authoritative npm lockfile
        working-directory: apps/web
        run: npm install --package-lock-only --ignore-scripts
      - name: Switch repository builds to npm ci
        shell: python
        run: |
          from pathlib import Path

          replacements = {
              Path("apps/web/Dockerfile"): [
                  ("COPY package.json ./", "COPY package.json package-lock.json ./"),
                  ("RUN npm install", "RUN npm ci"),
              ],
              Path("apps/web/Dockerfile.prod"): [
                  ("COPY package.json ./", "COPY package.json package-lock.json ./"),
                  ("RUN npm install", "RUN npm ci"),
              ],
              Path(".github/workflows/ci.yml"): [
                  ("run: npm install", "run: npm ci"),
              ],
              Path(".github/workflows/release.yml"): [
                  ("          npm install\n", "          npm ci\n"),
              ],
          }

          for path, edits in replacements.items():
              text = path.read_text()
              for old, new in edits:
                  if old not in text:
                      raise SystemExit(f"Expected text not found in {path}: {old!r}")
                  text = text.replace(old, new)
              path.write_text(text)

          readme = Path("README.md")
          text = readme.read_text()
          text = text.replace("cd apps/web\nnpm install\nnpm run dev", "cd apps/web\nnpm ci\nnpm run dev")
          readme.write_text(text)
      - name: Remove one-time generator workflows
        run: rm .github/workflows/generate-lockfile.yml .github/workflows/finalize-lockfile.yml
      - name: Commit finalized dependency lock
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "build(web): lock npm dependencies and use npm ci"
          git push
